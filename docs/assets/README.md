# Visual Assets

This folder holds brand assets referenced from the top level `README.md`.

## Expected files

| File | Purpose | Recommended size |
|---|---|---|
| `logo.png` | Main project logo shown at the top of the README | 512 x 512 PNG with transparent background |
| `logo-dark.png` (optional) | Dark mode variant | Same as above |
| `banner.png` (optional) | Wide banner for social previews | 1280 x 640 PNG |

## Placeholder

`logo.png` is not checked in yet. Until you drop a real file here, GitHub will show a broken image icon at the top of `README.md`. To add your logo:

1. Export your logo as a PNG at 512 x 512 with a transparent background
2. Save it as `docs/assets/logo.png`
3. Commit it

That is all that is needed. The main README already points at the right path.
